@page "{handler?}"
@model IndexModel
@{
    ViewData["Title"] = "Title";
}

<h1>Values</h1>
<ul>
    @foreach (var value in Model.Values)
    {
        <li>@value</li>
    }
</ul>
<form method="post">
    answer: <input asp-for="Value" />
    <button asp-page-handler="back">back</button>
    <button asp-page-handler="next">next</button>
</form>